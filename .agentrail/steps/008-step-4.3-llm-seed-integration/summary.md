Implemented fz-llm with Ollama HTTP client (retry/backoff, availability check) and prompt templates (seed generation, crash analysis, grammar inference per TargetKind). Wired 4-layer campaign pipeline: baseline(30%) -> LLM seeds(10%) -> mutation(40%) -> feedback(20%). LLM layer skips gracefully when Ollama unavailable. Added Clone to CaseInput. 148 total tests, zero clippy warnings.