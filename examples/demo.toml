[target]
name = "test-target"
kind = "cli"
entry = "./examples/test_target.sh"
input_mode = "stdin"
timeout_ms = 2000

[oracle]
success_exit_codes = [0]
failure_exit_codes = [1, 101]

[expectations]
must_not_panic = true
must_not_hang = true
must_not_segfault = true
